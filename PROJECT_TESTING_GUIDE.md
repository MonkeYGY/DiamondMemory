<!--
钻石记忆系统 - 统一测试规范（V0.9.1 起）
目标：开发迭代快、验证闭环稳、打包测试只在必要时触发。
-->

# 钻石记忆系统：统一测试规范（开发优先）

## 1. 核心原则
1) **开发态优先**：绝大多数优化内容用 `DM开发辅助` 一键测试验证，不要求每次都打包 App。  
2) **打包态兜底**：仅对“打包后才会出现/才算完成”的问题，补一次 dmg/exe 冒烟验证闭环。  
3) **最小可用闭环**：每次改动至少通过“健康检查 + 关键 API 链路”验证。  

---

## 2. 推荐测试流程（默认）

### 2.1 日常回归（每次开发改动后）
使用 `DM开发辅助`（按钮）：
1. **开发模式**：启动前端/后端（如已运行可跳过）  
2. **冒烟测试**：快速验证核心链路（health / 创建记忆 / 查询 / 知识库树）  
3. **一键查询**：查看后端与 Ollama 服务状态（排障用）  
4. 如异常：**一键清除** → **开发模式** → **冒烟测试** 复测  

> 冒烟测试脚本：`DM开发辅助/smoke_test.sh`

---

### 2.2 必须打包验证的任务类型（完成后至少跑 1 次）
以下类型仅开发态通过不算完成，必须补一次打包态冒烟：
1. **生产环境通信/安全策略**（典型：file://、Origin:null、CORS、IPC 代理）
2. **安装目录只读/资源路径**（resourcesPath、extraResources、写入权限）
3. **安装包产物完整性**（dmg/nsis、内嵌后端/ollama、首次启动体验）

对应版本优化项示例：
- ID=1、ID=2、ID=7（以及任何涉及 electron-builder / 资源路径的变更）

---

## 3. 冒烟测试验收清单（开发态）
### 必测项（自动化）
- 后端健康：`GET /health`
- 启动状态：`GET /api/startup-status`（允许 Ollama 缺失降级）
- 创建记忆：`POST /api/memory/create`
- 读取记忆：`GET /api/memory/get/{id}`
- 查询记忆：`GET /api/memory/query`
- 知识库树：`GET /api/knowledge/tree`（如过慢，优先处理“增量扫描/缓存”）

### 通过标准
1) 必测项全部成功；  
2) 若 Ollama 未安装或模型未下载，可允许 `startup-status` 显示 degraded，但记忆/知识库核心功能仍可用；  
3) 不允许出现“需要大模型才可打开核心页面”的硬依赖。  

---

## 4. 打包态冒烟测试清单（建议）
在 dmg/exe 安装后（尽量使用干净环境）：
1) 启动 App → 后端自动拉起  
2) 打开记忆页：创建/查询可用  
3) 打开知识库：树与文件读取可用  
4) 未下载模型时：UI 提示合理，核心功能可用  
5) 退出 App：后端与相关进程能受控关闭（不残留大量 runner）  

---

## 5. 常见问题排障（快速）
1) **端口占用**：先点“一键清除”，再点“开发模式”。  
2) **知识库树很慢**：优先处理 ID=6（增量扫描/缓存/分页）。  
3) **打包后接口全挂**：优先排查 file:// / Origin:null / CORS（ID=1）。  

