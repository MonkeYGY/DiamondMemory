<!--
钻石记忆系统 - 版本优化记录
版本：V0.9.1
说明：本文件用于跟踪本版本的优化项与完成情况（按时间倒序追加记录）。
-->

## 任务总览表（Backlog）
> 状态取值：`⏳ 待开始` / `🚧 进行中` / `✅ 已完成` / `⛔ 阻塞` / `🗑️ 取消`  
> 规则：每次追加“任务记录”时，必须同步更新本表状态，保持一致。

| ID | 优化项 | 优先级 | 状态 | 验收标准（简） |
|---:|---|:---:|:---:|---|
| 52 | 官网落地页（免备案上线准备）：补齐真实截图展示与放大预览、接入 Web Analytics、对接 macOS v1.0.0 下载、完善抖音/交流入口与首屏字号 | P2 | 🚧 进行中 | Cloudflare Pages 可直接部署 website/；截图支持5张并可点开大图；已接入 Web Analytics；macOS 下载可选择芯片并跳转 GitHub Releases；抖音按钮可跳转，交流按钮可提示；首屏字号更清晰 |
| 53 | 修复 Windows NSIS 打包配置：nsis include 路径指向 frontend/build（避免找不到 nsis-uninstaller.nsh） | P1 | ✅ 已完成 | Windows `electron-builder --win` 不再报 nsis-uninstaller.nsh 缺失；可继续生成 setup/portable |
| 54 | 修复 Windows NSIS 卸载脚本：MessageBox 字符串引号转义（避免 uninstaller.nsh 报语法错误） | P1 | ✅ 已完成 | Windows `electron-builder --win` 不再报 MessageBox usage/macro customUnInstall 错误；可继续生成 setup/portable |
| 55 | 修复 Windows 冒烟测试脚本 smoke_win_nsis.ps1：移除非 ASCII 文案并改为资源目录定位 exe（避免 PowerShell 编码解析报错） | P1 | ✅ 已完成 | Windows PowerShell 可执行脚本；自动定位安装后的 exe 并运行 smoke_app.mjs |
| 56 | 修复 Windows 打包后端资源路径：win extraResources 指向 dist/backend/main.dist 以落盘 DiamondMemoryBackend.exe | P1 | ✅ 已完成 | 安装后 `resources/backend/DiamondMemoryBackend.exe` 存在；smoke_app.mjs 产物检查通过 |
| 57 | 修复冒烟测试查询断言：兼容 /api/memory/query 返回无 total 字段（以 memories.length/total_candidates 判定） | P1 | ✅ 已完成 | Windows NSIS 冒烟测试创建后可通过检索校验并完成退出 |
| 58 | 修复 smoke_app.mjs 脚本在 Windows 上的编码解析问题：统一输出为 ASCII（避免 Node 解析报 SyntaxError） | P1 | ✅ 已完成 | Windows Node 可直接执行 smoke_app.mjs，不再因乱码字符导致 SyntaxError |
| 59 | 修复 Windows 设置页错误提示“需要安装 Ollama”：后端识别内置 resources/ollama 且 Electron 注入资源路径到后端环境变量 | P1 | ✅ 已完成 | 安装后设置页 /api/ollama/install-status 返回 installed=true；不再提示“下载 Ollama” |
| 51 | 修复 Windows 后端编译脚本 build-backend.bat：避免换行续写解析失败 + 提升 MSVC 编译稳定性（/Zm） | P1 | ✅ 已完成 | Windows 下 `npm run backend:build:win` 可正常运行并生成 DiamondMemoryBackend.exe |
| 50 | 完全卸载弹窗文案优化：明确“系统数据目录”与“工作区(存储路径)”口径，避免误解 | P2 | ✅ 已完成 | 选择“保留数据文件夹”时同时展示 userData 与用户设置的存储路径；文案明确不会删除工作区 |
| 49 | 模型库列表布局优化：将模型大小移动到下载按钮左侧，释放中间简介展示空间 | P3 | ✅ 已完成 | “约 x GB”显示在右侧靠近下载按钮；中间简介可展示更完整；不影响下载/已安装/下载中状态 |
| 43 | 首次安装引导：存储路径/备份路径/模型下载一体化弹窗（可跳过且不再提醒） | P1 | ✅ 已完成 | 首次安装启动弹窗引导；可完成存储路径、备份路径、模型下载；用户关闭/跳过后不再提醒 |
| 48 | 修复超长会话停更：自动缩短上下文窗口并在仅有思考输出时二次生成最终答案 | P0 | ✅ 已完成 | 对话过长时仍可稳定回复；不会卡在 thinking；历史对话仍完整保留可滚动查看；前端 build/test 通过 |
| 47 | 对话体验：流式输出不强制自动滚动，用户上滑可定住查看 | P0 | ✅ 已完成 | 流式生成过程中仅当用户接近底部时自动滚动；用户上滑后不再抢滚动位置；回到底部后自动恢复 |
| 46 | 对话体验：思考过程流式小字展示，完成后自动折叠并展示最终回答 | P0 | ✅ 已完成 | 流式过程中实时展示 thinking；done 后默认折叠 thinking；下方以正常字号展示最终回答；前后端 build/test 通过 |
| 45 | 修复对话无回复：兼容 Ollama 流式返回 thinking 字段（qwen 推理流） | P0 | ✅ 已完成 | qwen 类模型输出 thinking 时前端仍能收到 content 并展示；不再出现“AI对话无回复” |
| 44 | 整理上下文策略：默认 8K；显存足够时可用 16K；显存不足降级 4K | P0 | ✅ 已完成 | 整理/提炼类请求：VRAM>=8GB 选 16K；VRAM>=4GB 选 8K；否则选 4K；会话仍维持 2K/4K 性能优先 |
| 42 | 性能策略：会话上下文优先性能（2K/4K封顶），整理任务尽量固定8K | P0 | ✅ 已完成 | 会话时 num_ctx 仅在 2K/4K 间选择；整理/提炼等任务默认请求 8K（硬件不支持时自动降级） |
| 41 | 性能优化：对话默认输出上限收敛 + 动态上下文上限默认 8K + keep_alive 兼容旧值 | P0 | ✅ 已完成 | 对话响应耗时下降；模型不再因 keep_alive=0 反复加载；num_ctx 默认不超过 8K，且会在 2K/4K/8K 档位间自动匹配 |
| 40 | 退出清理：托管模式禁止后端自动启动 Ollama（避免退出残留 ollama/runner） | P0 | ✅ 已完成 | 彻底退出应用后不再残留由后端 Popen 拉起的 Ollama/runner；仅停止本应用启动的 Ollama |
| 39 | 缺大模型弹窗：每次启动只提示一次（关闭本次不再提示） | P1 | ✅ 已完成 | 检测到 LLM/核心模型缺失时弹窗仅出现一次；关闭后本次运行不再弹；重启应用会再次提示 |
| 38 | 模型常驻内存开关全局生效（切换/预加载/预热/Embedding） | P0 | ✅ 已完成 | 切换模型/预加载/预热/Embedding 均使用 keep_alive 配置；不再强制 OLLAMA_KEEP_ALIVE=-1；前后端 build 通过 |
| 37 | 设置页移除“重启并启动大模型”按钮 | P2 | ✅ 已完成 | 设置页不再展示重启按钮；前端 build 通过 |
| 36 | AI软件一键配置仅写入指导MD文件（不触发网关重启/不写cron/不改访问控制） | P1 | ✅ 已完成 | 配置/关闭操作不再执行 gateway restart，且只改 workspace 的 md 文件 |
| 35 | 修复对话页误判“Ollama 服务未启动”（startupStatus 注入 Ref 未解包） | P0 | ✅ 已完成 | 对话页/顶部状态/设置页一致；Ollama 已启动时不再误报 |
| 34 | 设置页“重启并启动大模型”按钮样式与“更多下载模型”一致 | P3 | 🗑️ 取消 | 该按钮已移除，无需再做样式统一 |
| 33 | 修复 AI软件集成中关闭 OpenClaw 后自动恢复开启（集成状态误判） | P1 | ✅ 已完成 | 关闭后刷新/重启应用不再自动变回开启 |
| 30 | 修复切换模型后进入对话提示"Ollama服务未启动" | P0 | ✅ 已完成 | 切换模型成功后重启应用，对话页可正常使用 |
| 31 | 设置页移除“AI引擎已就绪 / 大模型已下载完成”提示卡片 | P2 | ✅ 已完成 | 设置页不再展示两张完成态提示卡片，其他功能正常 |
| 1 | 修复生产环境前后端通信失败（CORS / Origin null / file 协议） | P0 | ✅ 已完成 | 打包后 App 内所有 API 正常；无 CORS 报错 |
| 2 | 禁止运行时写入安装目录：统一配置/数据落地到 userData，安装目录只读 | P0 | ✅ 已完成 | macOS/Windows 新装运行无写权限错误；配置可持久化 |
| 9 | 时序与冲突治理（Temporal + Deprecation）：有效期/废止/替代，检索默认只返回有效知识 | P0 | ✅ 已完成 | 偏好/事实更新后不再召回过期版本；可回溯历史 |
| 10 | MCP 接入体验产品化：工具 schema 稳定化 + 权限隔离 + 一键接入指引与自检 | P0 | ✅ 已完成 | 新用户按指引 5 分钟接入；权限可控；自检通过 |
| 3 | 后端向量引擎选择修正：MemoryService 使用 active vector store（Qdrant/FAISS fallback） | P1 | ✅ 已完成 | settings 选择 qdrant 时实际走 qdrant；不可用自动 fallback |
| 4 | 文档入库双管道：保真索引(A) + 结构化索引(B)；“免打扰”只走 A | P1 | ✅ 已完成 | 免打扰文档排版/内容不变入库；可检索可引用 |
| 5 | L2/L4/L6 去重策略升级：merge vs supersede（版本链/时序） | P1 | ✅ 已完成 | 同主题增量合并、事实更正可替代；历史可追溯 |
| 11 | L6 技能层产品化：技能版本/来源/适用条件 + 调用次数/反馈/成功率 + 自动升级触发 | P1 | ✅ 已完成 | 技能可评估、可演化；高频技能可自动迭代 |
| 6 | KnowledgeService 扫描 layer_filter 生效 + 大目录性能保护（增量扫描/缓存） | P2 | ✅ 已完成 | 大目录不卡顿；按层过滤真实生效 |
| 7 | 打包链路验证：mac dmg + win nsis 在“干净机”冒烟测试脚本 | P1 | ✅ 已完成 | 自动测试通过：启动/health/创建记忆/检索/退出 |
| 8 | 端口与配置统一：前端 port_config 与 ~/.diamond-memory/port.json 兼容 | P2 | ✅ 已完成 | 外部工具按约定可读取真实端口；无冲突 |
| 12 | Local-first 降级可用性：模型不可用时仍可检索/浏览/导出；耗时任务低功耗队列化 | P2 | ✅ 已完成 | 未装模型也能用“记忆/知识库”；任务不阻塞 UI |
| 13 | 安全与稳定性加固：高危 IPC 确认 + 备份跨平台/可等待 + 模型拉取输入校验 + 后台进程输出阻塞修复 | P0 | ✅ 已完成 | 高危操作需确认；备份可用且可等待；前端 build/后端 pytest 通过 |
| 14 | 深度安全加固（二期）：外链导航拦截 + SSRF 防护 + 知识库路径防穿越 + 配置路径服务端校验 + 安全回归测试 | P0 | ✅ 已完成 | 外链不在应用内打开；URL摄取拦截内网；路径越界被拒绝；pytest 通过 |
| 15 | 验收缺陷修复：开发模式多后端/多数据源导致数据对不上（统一单后端与端口权威） | P0 | ✅ 已完成 | OpenClaw/UI/命令行查询指向同一后端与同一 DB；重启开发模式数据一致 |
| 16 | 验收缺陷修复：OpenClaw 召回“用户偏好/我喜欢什么”失败（L1 兜底召回 + 中文关键词兜底） | P0 | ✅ 已完成 | 未触发整理也能召回近期 L1 关键细节；无嵌入模型时仍能用中文关键词命中 |
| 17 | 验收缺陷修复：右下角任务面板无法关闭/遮挡界面（增加最小化/关闭并记忆状态） | P1 | 🚧 进行中 | 可关闭/最小化；有任务自动弹出；任务结束自动隐藏；不遮挡 |
| 18 | 验收缺陷修复：采集中心上传失败 + 增加“免打扰(保真)”开关（前后端接口对齐） | P1 | 🚧 进行中 | 上传/网页采集成功；免打扰可用；错误提示明确 |
| 19 | 验收缺陷修复：用户手动拖入存储路径的文档无法自动入库（自动/一键知识库同步） | P1 | ✅ 已完成 | 文件放入后自动或一键进入数据库；知识库树即时可见；可检索可引用 |
| 20 | 验收缺陷修复：切换存储路径后知识库/图谱不同步、出现“访问被拒绝：路径不在允许范围内” | P0 | ✅ 已完成 | 切换后立刻可浏览知识库与图谱；无需快速整理；不再出现访问被拒绝/服务断开 |
| 21 | 体验修复：快速整理进度条按实时任务量推进（避免全程不动） | P1 | ✅ 已完成 | 快速整理过程中进度随批次/任务量更新；任务面板可看到持续变化 |
| 60 | 对话体验：问候检索仅取最近相似 2-3 条 + 超长上下文自动摘要写入 L1 + 清除对话新会话 + thinking 回灌止损 | P0 | ✅ 已完成 | 问候注入<=3条且更贴近近期；裁剪触发自动摘要并写入 L1；清空后不再发送旧上下文；thinking-only 不再导致 token 翻倍 |
| 22 | 打包产物清理与安装包测试回归（mac dmg） | P2 | ✅ 已完成 | 旧产物已清理，重新编译后端并产出 0.4.0 dmg/zip(x64/arm64) 共4个安装包，构建全部成功 |
| 23 | 安装包缺模型提示 + 模型下载并发/取消 + 修复 bge-m3 下载进度卡 0% | P0 | ✅ 已完成 | 缺模型时弹窗提示下载（可跳过）；支持并发下载与取消；进度正常推进 |
| 24 | 复用旧 Ollama 模型目录：无论模型在哪都能检测到；有则复用无则下载到 userData | P0 | ✅ 已完成 | 旧模型可被识别为已安装；新下载落到旧目录；无旧目录时落到 userData |
| 25 | 深度整理信息丢失修复：L4/L6去重阈值过低+合并策略过猛+分类归并过激 | P0 | ✅ 已完成 | 深度整理后L3-L6信息不再大量丢失；阈值可配置 |
| 26 | AI软件一键配置改为增量写入：保留原生配置内容，使用标记区间注入/移除 | P0 | ✅ 已完成 | 配置后原生内容完整保留；取消配置后原生内容完整恢复 |
| 27 | 完全卸载功能失败根因修复：后置清理脚本+Ollama进程等待退出+防双重关闭+删除重试+IPC响应保障 | P0 | ✅ 已完成 | 完全卸载后所有数据目录被清理；无EBUSY错误；应用正常退出 |
| 28 | 整理规则对齐OpenClaw标准：所有阈值统一0.85+合并改为保真拼接+分类归并保守化 | P0 | ✅ 已完成 | 整理流程与OpenClaw保真写入策略一致；信息零丢失 |
| 29 | Ollama下载进度卡0%修复：重定向URL导致content-length丢失+进度更新条件过严 | P0 | ✅ 已完成 | 下载时实时显示已下载大小；无总大小时显示indeterminate动画 |

---

## ✅ 本次版本优化任务记录（时间倒序）

### 2026-05-01
- 任务类型：优化（对话体验）
- 任务简述：对话链路新增“问候类检索限额（最近相似 2-3 条）+ 超长上下文裁剪自动摘要并写入 L1 + 清除对话重置会话新开 + thinking 回灌止损”，降低无关记忆注入与 token 膨胀，避免简单问候触发历史全量复盘导致超窗。
- 修改文件列表：
  - `backend/app/config/settings.py`
  - `backend/app/api/chat_routes.py`
  - `backend/app/services/retrieval_service.py`
  - `backend/tests/test_chat_intent_and_greeting_retrieval.py`
  - `backend/tests/test_retrieval_recent_similar.py`
  - `backend/tests/test_chat_summary_prompt.py`
  - `frontend/src/renderer/api/backend.ts`
  - `frontend/src/renderer/api/backend.test.ts`
  - `frontend/src/renderer/utils/prompt-window.ts`
  - `frontend/src/renderer/utils/prompt-window.test.ts`
  - `frontend/src/renderer/views/ChatView.vue`
  - `docs/superpowers/specs/2026-05-01-chat-context-budget-and-auto-summary-design.md`
  - `docs/superpowers/plans/2026-05-01-chat-context-budget-and-auto-summary.md`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：修复（体验 / Windows）
- 任务简述：修复 Windows 安装后设置页仍提示“需要下载 Ollama”：Electron 启动后端时注入 `ELECTRON_RESOURCES_PATH/RESOURCE_PATH=process.resourcesPath`，后端 `OllamaDownloadService.is_installed()` 启动时尝试从 resources 部署内置 ollama 并据此返回 installed=true，避免误导用户重复下载。
- 修改文件列表：
  - `frontend/src/main/backend-manager.ts`
  - `backend/app/services/ollama_download_service.py`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：修复（测试链路 / Windows）
- 任务简述：修复 `smoke_app.mjs` 在 Windows 上因编码/乱码字符导致 Node 抛 `SyntaxError: Invalid or unexpected token`：将脚本输出与关键错误信息统一为 ASCII，避免用户在 PowerShell 中读取/回写导致 UTF-8 被破坏后无法执行。
- 修改文件列表：
  - `scripts/smoke/smoke_app.mjs`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：修复（测试链路 / Windows）
- 任务简述：修复冒烟测试在创建记忆后检索断言失败：/api/memory/query 返回结构不包含 total 字段时，改为使用 `memories.length`（并兼容 `total_candidates`）判断检索成功，避免误判失败。
- 修改文件列表：
  - `scripts/smoke/smoke_app.mjs`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：修复（打包链路 / Windows）
- 任务简述：修复 Windows 安装包内后端资源路径不匹配：Nuitka 产物落在 `dist/backend/main.dist/`，但运行期与冒烟测试期望 `resources/backend/DiamondMemoryBackend.exe`。将 Windows 打包配置 `win.extraResources` 改为从 `../dist/backend/main.dist/` 复制到 `backend/`，确保 exe 直接落盘到 `resources/backend/` 根目录。
- 修改文件列表：
  - `frontend/electron-builder.yml`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：修复（测试链路 / Windows）
- 任务简述：修复 Windows PowerShell 执行 `smoke_win_nsis.ps1` 报编码/解析错误：移除 emoji/非 ASCII 输出，并改为通过 `resources\\backend` 自动定位安装后的应用 exe（避免依赖中文文件名路径）。
- 修改文件列表：
  - `scripts/smoke/smoke_win_nsis.ps1`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：修复（打包链路 / Windows）
- 任务简述：修复 NSIS 打包报错 `MessageBox usage` / `macro customUnInstall`：对卸载确认弹窗文案中的引号进行 `$\"` 转义，并将卸载目录删除改为 `RMDir /r`，避免脚本语法错误导致 makensis 中止。
- 修改文件列表：
  - `frontend/build/nsis-uninstaller.nsh`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：修复（打包链路 / Windows）
- 任务简述：修复 Windows NSIS 打包报错 `cannot find ... nsis-uninstaller.nsh`：将 electron-builder 的 `nsis.include` 从 `../build/nsis-uninstaller.nsh` 修正为 `build/nsis-uninstaller.nsh`，确保从 `frontend/build/` 正确加载 NSIS 脚本。
- 修改文件列表：
  - `frontend/electron-builder.yml`
- 完成状态：✅ 已完成

---

### 2026-05-01
- 任务类型：优化（官网/分发）
- 任务简述：进一步增大首页首屏核心文案字号与按钮/徽章/统计字号，提升在大屏上的可读性与聚焦度。
- 修改文件列表：
  - `website/style.css`
  - `website/index.html`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：🚧 进行中（等待你将最新样式文件同步到 GitHub 官网仓库并强刷验证线上效果）

---

### 2026-05-01
- 任务类型：优化（官网/分发）
- 任务简述：完善“关注与交流”入口：抖音关注按钮直连到抖音主页；用户交流按钮改为弹窗提示“进入抖音主页点击群入口加入”；修复“获取进群方式”按钮默认态文字/边框对比度过低的问题；同时整体加大首屏（导航/主标题/副标题/按钮/统计）字号与版心宽度，提升可读性与转化。
- 修改文件列表：
  - `website/style.css`
  - `website/script.js`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：🚧 进行中（等待你将最新网页文件同步到 GitHub 官网仓库并验证线上生效）

---

### 2026-05-01
- 任务类型：优化（官网/分发）
- 任务简述：补齐官网“截图展示”区域：替换占位文字为 5 张真实界面截图（仪表盘/记忆管理/AI对话/知识库/知识图谱），并支持点击放大预览；移除页脚邮箱对外联系方式，保持页面更轻量与低风险。截图文件采用相对路径 `assets/screenshots/*.png`，由仓库提供静态资源。
- 修改文件列表：
  - `website/index.html`
  - `website/style.css`
  - `website/script.js`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：🚧 进行中（等待你把截图文件上传到仓库并更新 Pages 部署）

---

### 2026-05-01
- 任务类型：优化（官网/分发）
- 任务简述：对接 macOS v1.0.0 GitHub Releases 下载：macOS 下载按钮改为弹窗选择芯片（Apple Silicon/Intel），分别跳转到对应 dmg；为站点接入 Cloudflare Web Analytics（仅统计访问，不做软件内数据收集）。
- 修改文件列表：
  - `website/index.html`
  - `website/style.css`
  - `website/script.js`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：🚧 进行中（等待你将最新网页文件同步到 GitHub 仓库并验证线上可下载）

---

### 2026-04-30
- 任务类型：优化（官网/分发）
- 任务简述：官网落地页按“免备案、低成本、低合规风险”方向调整：移除登录/注册与本地存储伪登录态，避免用户误解为账号收集；移除 Google Fonts 外链改为系统字体；新增“关注与交流”板块引导抖音关注与进群（不挡下载）；补充隐私说明与免责声明；下载入口预留为 GitHub Releases 链接。
- 修改文件列表：
  - `website/index.html`
  - `website/style.css`
  - `website/script.js`
- 完成状态：🚧 进行中（等待配置 GitHub Releases 下载链接并完成 Cloudflare Pages 部署）

---

### 2026-04-30
- 任务类型：修复/优化（打包链路 / Windows）
- 任务简述：修复 Windows 下 `npm run backend:build:win` 执行失败：将 bat 中依赖 `^` 的多行续写改为单行命令，避免因行尾/编码导致参数被拆成独立命令；同时为 MSVC 编译注入 `CL=/Zm200` 提升大文件（如 faiss swig 包装）编译稳定性。
- 修改文件列表：
  - `scripts/build-backend.bat`
- 完成状态：✅ 已完成

---

### 2026-04-30
- 任务类型：优化
- 任务简述：优化“确认完全卸载”弹窗文案：解释清楚“系统数据目录（userData）”与“用户设置的工作区/存储路径”是两套概念，并在选择“保留数据文件夹”时同时展示两者路径，避免误认为数据会保留到工作区。
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm -C frontend run build` 通过）

---

### 2026-04-30
- 任务类型：构建/打包（安装包）
- 任务简述：清理旧打包产物（dist/backend、dist/electron），重新编译 Python 后端（Nuitka 4.0.8 + Python 3.11，编译 2066 个 C 文件，ccache 命中率 2051/2066），并重新打包 Electron macOS 安装包（electron-builder 24.13.3）。产出 x64/arm64 双架构 dmg/zip 共4个安装包，构建全部成功。
- 修改文件列表：
  - `dist/backend/main.dist/*`（重新生成）
  - `dist/electron/钻石记忆系统-0.4.0-mac-x64.dmg`（229MB，重新生成）
  - `dist/electron/钻石记忆系统-0.4.0-mac.zip`（220MB，重新生成）
  - `dist/electron/钻石记忆系统-0.4.0-mac-arm64.dmg`（222MB，重新生成）
  - `dist/electron/钻石记忆系统-0.4.0-arm64-mac.zip`（213MB，重新生成）
  - 对应 *.blockmap 文件
- 完成状态：✅ 已完成（backend: Nuitka 编译成功；frontend: electron-builder 打包成功，exit code 0）

---

### 2026-04-30
- 任务类型：修复/优化
- 任务简述：修复“模型库弹窗/可选模型列表”中间简介不显示或过度被挤压的问题：移除多余的 spacer 抢占宽度；下载行改为 size 使用 `margin-left:auto` 靠右并紧贴下载/状态区域；同时让模型名称可收缩省略（ellipsis），把更多空间留给简介文本。
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm -C frontend run build` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：优化“模型库”下载列表布局：将“约 x GB”移动到右侧下载按钮左侧，释放中间更大空间用于显示模型简介，提升可读性。
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm -C frontend run build` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：修复“AI会话超长后停止思考无法回复”：前端发送对话时自动按 token 预算裁剪上下文窗口（历史仍保留可滚动），避免提示词过长导致模型截断/异常；同时当模型仅输出 thinking 而未输出最终 content 时，自动触发一次“仅生成最终答案”的二次请求，将思考折叠并输出正常字号的最终回答。
- 修改文件列表：
  - `frontend/src/renderer/views/ChatView.vue`
- 完成状态：✅ 已完成（frontend: `npm -C frontend run build`、`npm -C frontend test` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：优化流式对话滚动体验：新增“自动滚动锁”逻辑，流式生成时仅在用户接近底部（阈值 120px）时自动滚动；用户上滑查看历史内容时不再强制拉回底部；滚动回底部附近后自动恢复跟随。
- 修改文件列表：
  - `frontend/src/renderer/views/ChatView.vue`
- 完成状态：✅ 已完成（frontend: `npm -C frontend run build`、`npm -C frontend test` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：实现“思考过程流式展示 + 完成后折叠 + 最终答案正常字号展示”。前端流式解析同时消费 `message.thinking` 与 `message.content`，思考以小字实时输出并在 done 后默认折叠；若模型在部分 chunk 中 content 为空但 thinking 有值，仍可持续展示。后端保留 thinking 字段并兼容旧客户端（content 为空时同步填充 content=thinking，但不移除 thinking）。
- 修改文件列表：
  - `frontend/src/renderer/api/backend.ts`
  - `frontend/src/renderer/views/ChatView.vue`
  - `backend/app/services/inference/inference_service.py`
- 完成状态：✅ 已完成（frontend: `npm -C frontend run build`、`npm -C frontend test` 通过；backend: `python -m compileall backend/app backend/main.py` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：修复“AI对话无回复”：qwen 类推理模型在 Ollama 流式输出中使用 `message.thinking` 增量返回，`message.content` 可能为空，导致前端仅读取 content 时无任何显示。后端在转发流式数据时将 thinking 归一为 content（仅当 content 为空且 thinking 非空时），并兼容非流式返回的 thinking 字段。
- 修改文件列表：
  - `backend/app/services/inference/inference_service.py`
- 完成状态：✅ 已完成（本地复现：`curl /api/chat/stream` 返回 thinking；修复后返回 content，前端可展示）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：细化整理/提炼类上下文策略：默认尽量使用 8K；当 VRAM>=8GB 时允许提升到 16K；当 VRAM<4GB 或无法识别 VRAM 时自动降级为 4K。会话仍维持 2K/4K 性能优先策略。
- 修改文件列表：
  - `backend/app/services/inference/inference_service.py`
- 完成状态：✅ 已完成（backend: `python -m compileall backend/app/services/inference/inference_service.py` 通过）

---

### 2026-04-30
- 任务类型：新增/优化
- 任务简述：新增“首次安装引导”弹窗：首次安装打开软件后引导用户设置存储路径、备份路径并完成模型下载；用户选择跳过或关闭后不再提醒。同时优化“缺大模型提示”策略：缺模型时每次启动仅提示一次，关闭/本次跳过后本次运行不再重复弹出。
- 修改文件列表：
  - `frontend/src/main/index.ts`
  - `frontend/src/preload/index.ts`
  - `frontend/src/renderer/types/electron.d.ts`
  - `frontend/src/renderer/App.vue`
  - `frontend/src/renderer/components/InitialSetupWizard.vue`
  - `frontend/src/renderer/components/ModelSetup.vue`
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm -C frontend run build`、`npm -C frontend test` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：调整“动态上下文档位选择”策略：会话场景偏性能优先（估算更保守，num_ctx 仅在 2K/4K 间选择并封顶 4K）；整理/提炼等任务优先稳定性，默认尽量请求 8K（硬件/配置不支持时自动降级）。
- 修改文件列表：
  - `backend/app/services/inference/inference_service.py`
- 完成状态：✅ 已完成（backend: `python -m compileall backend/app/services/inference/inference_service.py` 通过；动态上下文脚本验证：chat_short=2K、chat_long=4K、task=8K）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：修复“对话回复慢/风扇狂转”的核心配置：1) 兼容旧 keep_alive=0/false，启动时自动迁移为 5m，避免每次请求都卸载重载模型；2) 修复动态上下文 VRAM 档位判断错误，并将动态 num_ctx 上限默认约束为 8K；3) 将对话接口默认 max_tokens（输出上限）收敛为 2048，降低无意义长输出带来的负载；4) 修复设置页 keep_alive 开关状态判定（-1 才代表常驻）。
- 修改文件列表：
  - `backend/app/services/inference/inference_service.py`
  - `backend/app/services/embedding_service.py`
  - `backend/main.py`
  - `backend/app/config/settings.py`
  - `backend/app/api/chat_routes.py`
  - `backend/app/api/inference.py`
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（backend: `python -m compileall backend/app backend/main.py` 通过；frontend: `npm -C frontend run build` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：修复“彻底退出后仍残留 Ollama/runner”的关键路径：在 Electron 托管模式下注入环境变量，并让 Python 后端禁用 `subprocess.Popen(["ollama","serve"])`（含 /api/ollama/start）自动拉起 detached Ollama，避免退出后残留进程。
- 修改文件列表：
  - `frontend/src/main/backend-manager.ts`
  - `backend/app/services/inference/inference_service.py`
  - `backend/app/services/ollama_download_service.py`
  - `backend/tests/test_ollama_autostart_guard.py`
- 完成状态：✅ 已完成（backend: `PYTHONPATH=... python -m unittest backend.tests.test_ollama_autostart_guard -v` 通过；frontend: `npm run electron:build-main` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：优化“缺大模型/核心模型”弹窗提示策略：每次启动检测到缺模型时仅提示一次；用户关闭后本次运行不再重复弹出；重新打开应用后会再次提示一次。
- 修改文件列表：
  - `frontend/src/renderer/App.vue`
- 完成状态：✅ 已完成（frontend: `npm run build` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：修复“模型常驻内存”开关在切换大模型后不一定生效的问题：统一所有 Ollama 请求（切换/预加载/预热/聊天/Embedding）使用配置 keep_alive；并移除启动 Ollama 时强制设置 OLLAMA_KEEP_ALIVE=-1，避免覆盖开关语义与造成多模型常驻带来的性能问题。
- 修改文件列表：
  - `backend/app/api/config_routes.py`
  - `backend/app/services/inference/inference_service.py`
  - `backend/app/services/embedding_service.py`
  - `backend/app/services/ollama_download_service.py`
  - `backend/main.py`
  - `frontend/src/main/backend-manager.ts`
- 完成状态：✅ 已完成（backend: `python -m compileall backend` 通过；frontend: `npm run build` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：设置页移除“重启并启动大模型”按钮，避免与“切换模型自动重启”逻辑重复。
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm run build` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：调整 AI软件一键配置/关闭逻辑：仅注入/移除 OpenClaw/Qclaw/Hermes 的指导 MD 文件，不再触发 gateway restart，也不再写入 cron 或修改来源访问控制，避免对网关运行状态造成影响。
- 修改文件列表：
  - `backend/app/services/openclaw_service.py`
  - `backend/app/services/qclaw_service.py`
  - `backend/app/services/hermes_service.py`
- 完成状态：✅ 已完成（backend: `python -m py_compile`、`pytest -q backend/tests/test_openclaw_integration_detection.py` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：修复对话页长期误报“Ollama 服务未启动”的根因（startupStatus 通过 provide 传 Ref，但 ChatView/SettingsView inject 后按普通对象读取，导致 ollama_ready 恒为 falsy）；并统一设置页的 Ollama 连接状态口径，避免和对话页检测不一致。
- 修改文件列表：
  - `frontend/src/renderer/views/ChatView.vue`
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm test`、`npm run build` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：设置页“重启并启动大模型”按钮调整为与上方“更多下载模型”一致的虚线按钮样式，统一视觉与交互。
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm run build`、`npm test` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：修复 AI软件集成中关闭 OpenClaw 后又自动显示为开启的问题（根因：集成状态检测误判，将普通文本中的“钻石记忆系统”等字样当作已集成）。
- 修改文件列表：
  - `backend/app/services/openclaw_service.py`
  - `backend/tests/test_openclaw_integration_detection.py`
- 完成状态：✅ 已完成（backend: `pytest -q backend/tests/test_openclaw_integration_detection.py` 通过）

---

### 2026-04-30
- 任务类型：优化
- 任务简述：设置页移除“AI引擎已就绪 / 大模型已下载完成”两张完成态提示卡片，减少冗余信息。
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：✅ 已完成（frontend: `npm run build` 通过）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：完整修复模型切换后状态丢失、重启恢复默认模型、Ollama未启动等3个关联问题。
  - **问题1**：手动切换模型到qwen2.5:1.5b后，对话显示"Ollama服务未启动"
  - **问题2**：点击重启后又默认加载qwen3.5:4b（用户选择的模型丢失）
  - **问题3**：对话界面持续提示Ollama未启动
  
  **根因分析**：
  - 启动时直接使用 settings.local_llm_model 默认值（qwen3.5:4b），未从数据库读取用户保存的配置
  - switch-model API 在后台线程执行时未实时更新 startupStatus
  - 切换模型时会先卸载旧模型（keep_alive=0），但新模型加载期间状态显示为"未启动"

  **修复方案**：
  1. 启动预加载前从数据库读取 local_llm_model 并应用到 settings（main.py）
  2. switch-model API 实时更新 startup_runtime 状态（config_routes.py）
  3. 增强错误处理和日志，超时从30s增加到120s

- 修改文件列表：
  - `backend/main.py`（第111-115行：增加从数据库读取 local_llm_model 的逻辑）
  - `backend/app/api/config_routes.py`（switch-model API 增加实时状态更新 + 增强日志）
- 完成状态：✅ 已完成（TypeScript类型检查通过：vue-tsc --noEmit）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：修复设置页面切换模型后进入对话页面提示"Ollama服务未启动"的问题。根本原因是切换模型后后端在后台加载模型，但前端 startupStatus 未及时刷新，导致对话页面误判服务状态。
  - **根因分析**：`switchLocalModel()` 调用后端 API 后仅延迟 3 秒刷新状态，模型实际加载时间不确定，用户可能在模型加载完成前进入对话页面
  - **修复方案**：切换模型成功后显示"重启应用后生效"提示，然后调用 `window.electronAPI.relaunchApp()` 触发完整应用重启，确保后端/模型/端口完全按新模型重新初始化
  - **与现有方案一致性**：与 `handleRestartForModels()` 和 `handleSelectStoragePath()` 的重启策略保持一致，均使用 relaunchApp 实现全量重启
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`（switchLocalModel 函数改为显示重启提示后调用 relaunchApp）
- 完成状态：✅ 已完成

---

### 2026-04-30
- 任务类型：优化
- 任务简述：优化模型下载界面用户体验，默认只显示2个推荐模型（bge-m3必选 + qwen3.5:4b推荐），其余通过"🔍 更多下载模型"按钮在弹窗中展示完整模型库。避免一次性显示过多模型导致界面拥挤。
  - **UI改进**：3处模型列表统一改为只显示前2个模型
  - **交互优化**：每处模型列表后添加"🔍 更多下载模型"按钮，点击打开已有模型库弹窗
  - **代码质量**：新增 `visibleDefaultModels` 计算属性，复用已有的 `showModelLibrary` 弹窗和 `ollamaLibrary` 数据源
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`（新增visibleDefaultModels计算属性 + 3处defaultModels改为visibleDefaultModels + 统一按钮文案为"更多下载模型"）
- 完成状态：✅ 已完成（TypeScript类型检查通过：vue-tsc --noEmit）

---

### 2026-04-30
- 任务类型：修复
- 任务简述：Ollama 引擎安装全链路修复——从"下载进度卡0%"到"引擎无法启动"的完整问题链修复。共发现并修复 4 层叠加问题：
  - **L1 网络层**：用户网络环境下 GitHub/GitHub镜像全部不可达（ConnectTimeout），下载请求卡死在连接阶段，数据永远为 0
  - **L2 进度更新逻辑**：`_do_download` 中 `if total_size > 0:` 将 progress/downloaded/total 全部包裹，content-length 为空时整个跳过；改用 Session 显式重定向 + downloaded/total 无条件外提 + progress=-1 兜底
  - **L3 空壳文件**：历史失败留下 0 字节空壳 ollama 文件，`is_installed()` 仅检查 exists+X_OK 误判为已安装；`spawn` 空文件 → error(-8) 启动失败。增加 `stat().st_size > 0` 校验 + 自动清理空壳 + 从内置路径重新部署
  - **L4 内置部署缺失**：应用打包时已有 build/ollama/mac/ollama(74MB) 通过 extraResources 打入 .app，但代码从未使用它。新增 `_ensure_bundled_ollama()`（后端）和 `deployBundledOllama()`（前端），启动时自动检测并部署内置二进制，彻底绕过网络依赖
- 修改文件列表：
  - `backend/app/services/ollama_download_service.py`（Session显式重定向；downloaded/total无条件更新；progress=-1兜底；timeout改为(15,600)双超时；_ensure_bundled_ollama: 大小校验+空壳清理+内置路径自动部署；网络错误macOS下提示brew安装）
  - `frontend/src/main/backend-manager.ts`（getOllamaPath: embedded路径加大小校验；startOllama: hasValidLocalOllama大小校验+空壳清理+deployBundledOllama自动部署；新增deployBundledOllama方法）
  - `frontend/src/renderer/components/OllamaSetup.vue`（progress<0时indeterminate动画+条件格式化显示）
- 完成状态：✅ 已完成（dev模式验证：Ollama v0.21.0 从内置74MB二进制成功部署→启动→Metal GPU就绪→API正常）

### 2026-04-29
- 任务类型：修复
- 任务简述：完全卸载功能失败根因排查与修复。5个根因：①应用运行时直接fs.rmSync(userDataPath)因文件句柄占用导致EBUSY ②macOS上Ollama进程SIGTERM后未等待退出，模型文件锁未释放 ③app:uninstall和before-quit双重gracefulShutdown ④removeDir无重试机制 ⑤app.quit()后IPC响应丢失。修复：用后置清理脚本(detached进程)替代运行时删除userDataPath；Ollama SIGTERM后等待exit事件+8秒SIGKILL兜底；新增isUninstalling标志防双重关闭；removeDirWithRetry支持3次重试；IPC响应后延迟800ms再app.exit(0)；Windows/macOS双平台清理脚本。
- 修改文件列表：
  - `frontend/src/main/index.ts`（重写app:uninstall处理器：后置清理脚本方案+isUninstalling标志+removeDirWithRetry+延迟退出；修改before-quit跳过卸载场景）
  - `frontend/src/main/backend-manager.ts`（macOS上Ollama进程SIGTERM后等待exit事件，8秒超时SIGKILL兜底）
  - `frontend/src/renderer/views/SettingsView.vue`（调整卸载结果提示文案，反映后置清理机制）
- 完成状态：✅ 已完成

### 2026-04-29
- 任务类型：优化
- 任务简述：整理规则全面对齐OpenClaw标准。核心改造：①所有去重/相似/分类归并阈值统一从各自不同值提升到0.85（与OpenClaw的dedup_threshold一致）②_merge_summary()和_merge_skill()从LLM重写改为保真简单拼接（与OpenClaw L1→L2的old+"\n\n"+new策略一致）③merge_output_ratio从1.2提升到1.5④L6去重增加同分类约束。设计哲学从"积极合并压缩重写"转为"保真写入保守合并检索时智能过滤"。
- 修改文件列表：
  - `backend/app/config/settings.py`（6个阈值统一为0.85，merge_output_ratio提升到1.5）
  - `backend/app/services/memory_service.py`（7处getattr fallback值更新为0.85；_merge_summary()改为保真拼接；_merge_skill()改为保真拼接；L6去重增加同分类约束）
  - `backend/app/services/category_normalization_service.py`（getattr fallback值更新为0.85）
- 完成状态：✅ 已完成

### 2026-04-29
- 任务类型：优化
- 任务简述：AI软件一键配置改为增量写入模式。三个服务（OpenClaw/Qclaw/Hermes）的MEMORY.md从全量覆盖改为标记区间增量注入，HEARTBEAT.md/BOOTSTRAP.md/SOUL.md从正则替换改为标记区间注入。配置时原生内容完整保留在标记区间下方，取消配置时仅移除标记区间内容、原生内容完整恢复。无标记时回退到备份恢复。
- 修改文件列表：
  - `backend/app/services/openclaw_service.py`（新增DM_SECTION_START/END标记常量、_inject_section/_remove_section辅助方法；_modify_agent_instructions改为增量注入；_restore_agent_instructions改为标记移除；is_agent_integrated增加标记检测；MEMORY_MD_TEMPLATE"备用记忆"描述更新）
  - `backend/app/services/qclaw_service.py`（新增DM_SECTION_START/END标记常量、_inject_section/_remove_section辅助方法；_modify_memory_md改为增量注入；_modify_heartbeat_md改为增量注入；_restore_workspace_files改为标记移除；is_diamond_memory_integrated增加标记检测；MEMORY_MD_TEMPLATE"备用记忆"描述更新）
  - `backend/app/services/hermes_service.py`（新增DM_SECTION_START/END标记常量、_inject_section/_remove_section辅助方法；_modify_soul_md改为增量注入；_modify_memory_md改为增量注入；_restore_soul_md/_restore_memory_md改为标记移除；is_diamond_memory_integrated增加标记检测；MEMORY_MD_TEMPLATE"备用"描述更新）
- 完成状态：✅ 已完成

### 2026-04-29
- 任务类型：优化
- 任务简述：修复深度整理对L3-L6内容合并过猛导致信息丢失的问题。7项核心改善：①L4去重阈值0.72→0.82 ②L6去重阈值0.55→0.75 ③L6去重增加同分类约束 ④_merge_summary()从全面重写改为增量追加 ⑤_merge_skill()从全面重写改为增量追加 ⑥输出长度限制从80%提高到120% ⑦分类归并模糊匹配阈值0.65→0.80。所有阈值配置化移至settings.py。
- 修改文件列表：
  - `backend/app/config/settings.py`（新增6个可配置阈值：l4_dedup_threshold/l6_dedup_threshold/l2_to_l4_similarity_threshold/l4_to_l6_similarity_threshold/merge_output_ratio/category_fuzzy_similarity_threshold）
  - `backend/app/services/memory_service.py`（移除硬编码阈值L4_DEDUP_THRESHOLD/L6_DEDUP_THRESHOLD；5处0.70硬编码改为从settings读取；_merge_summary()和_merge_skill()改为增量追加模式+长度安全检查；L6去重增加同分类约束）
  - `backend/app/services/category_normalization_service.py`（移除硬编码FUZZY_SIMILARITY_THRESHOLD，改为从settings读取category_fuzzy_similarity_threshold）
- 完成状态：✅ 已完成

### 2026-04-29
- 任务类型：构建/打包（安装包）
- 任务简述：根据最新代码重新编译 Nuitka 后端产物并重新打包 mac dmg/zip（x64/arm64），用于后续干净机安装与首次启动回归验证。
- 修改文件列表：
  - `dist/backend/main.dist/*`（重新生成）
  - `dist/electron/钻石记忆系统-0.4.0-*.dmg*`（重新生成）
  - `dist/electron/钻石记忆系统-0.4.0-*.zip*`（重新生成）
- 完成状态：✅ 已完成

---

### 2026-04-29
- 任务类型：优化/测试（模型目录复用）
- 任务简述：实现“无论模型在哪都能检测到”：启动 Ollama 前自动探测旧模型目录（优先环境变量 OLLAMA_MODELS，其次按系统默认路径），若存在且 manifests 非空则直接复用（不迁移）；若不存在则使用 userData/ollama-models。并补充 Vitest 单测覆盖“旧目录优先/无旧目录回退”两种场景。
- 修改文件列表：
  - `frontend/src/main/backend-manager.ts`
  - `frontend/src/main/ollama-model-dir.test.ts`
  - `frontend/src/renderer/api/backend.ts`（增强 localStorage 兜底，确保测试环境稳定）
  - `docs/superpowers/specs/2026-04-29-ollama-model-dir-reuse-design.md`
  - `docs/superpowers/plans/2026-04-29-ollama-model-dir-reuse.md`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test`、`npm run build` 通过）

---

### 2026-04-29
- 任务类型：修复/优化/测试（模型下载体验）
- 任务简述：修复“bge-m3 点击下载进度长期 0% / 只能下载一个 / 无取消按钮 / 首次启动缺模型无引导”的问题：后端改为使用 Ollama `/api/pull` 流式进度采集（不再依赖命令行解析），支持多模型并发下载与取消；前端下载列表支持并发与取消，并新增“缺模型下载引导”弹窗（可跳过并跳转设置页）。
- 修改文件列表：
  - `backend/app/api/config_routes.py`
  - `backend/tests/test_startup_status_api.py`
  - `backend/tests/test_model_pull_api.py`
  - `frontend/src/renderer/views/ModelView.vue`
  - `frontend/src/renderer/views/SettingsView.vue`
  - `frontend/src/renderer/App.vue`
  - `frontend/src/renderer/components/ModelSetup.vue`
  - `docs/superpowers/plans/2026-04-29-model-download-ux-and-concurrency.md`
- 完成状态：✅ 已完成（backend: 相关 pytest 通过；frontend: `npm run build` 通过）

---

### 2026-04-29
- 任务类型：测试/修复（打包链路）
- 任务简述：完成 mac 安装包产出：修复 universal 合并冲突、修复 codesign entitlements 路径错误后，成功生成 mac x64/arm64 双架构安装包与 zip，并生成对应 blockmap，进入下一阶段“安装包冒烟测试”。
- 修改文件列表：
  - `frontend/electron-builder.yml`
- 产物清单（示例路径以 dist/electron 为准）：
  - `钻石记忆系统-0.4.0-mac-x64.dmg`
  - `钻石记忆系统-0.4.0-mac-arm64.dmg`
  - `钻石记忆系统-0.4.0-mac.zip`
  - `钻石记忆系统-0.4.0-arm64-mac.zip`
  - `*.blockmap`
- 完成状态：🚧 进行中（等待安装/首次启动/health/退出清理冒烟结果）

---

### 2026-04-29
- 任务类型：修复（打包链路）
- 任务简述：修复 mac 打包 codesign 失败：electron-builder 配置的 `entitlements/entitlementsInherit` 指向 `../build/entitlements.mac.plist`，但实际 entitlements 文件位于 `frontend/build/entitlements.mac.plist`，导致 codesign 报 `cannot read entitlement data`。修正为 `build/entitlements.mac.plist` 后可继续签名并完成 dmg 产出。
- 修改文件列表：
  - `frontend/electron-builder.yml`
- 完成状态：✅ 已完成（等待出包机重新执行 `npm run electron:build:mac` 验证产物）

---

### 2026-04-29
- 任务类型：维护（打包产物清理）
- 任务简述：为避免误用旧的 `.app` 目录进行安装/测试，清理 `dist/electron/mac/` 与 `dist/electron/mac-arm64/` 下历史 `钻石记忆系统.app` 目录，后续以本次重新打包生成的 dmg/zip 产物为准进行冒烟测试。
- 修改文件列表：
  - `dist/electron/mac/钻石记忆系统.app`（删除）
  - `dist/electron/mac-arm64/钻石记忆系统.app`（删除）
- 完成状态：✅ 已完成

---

### 2026-04-29
- 任务类型：修复/优化（打包链路）
- 任务简述：修复 mac 打包脚本 `electron:build:mac` 失败：此前配置为 `arch: universal`，electron-builder 会执行 x64/arm64 打包后再用 `@electron/universal` 合并为 universal App；但我们将 Nuitka 后端产物作为 `extraResources` 打入 `Contents/Resources/backend/...`，该后端二进制不是 universal，导致合并阶段报错（检测到同名 Mach-O 文件在两份包内一致但未声明 x64ArchFiles）。调整策略为：不构建 universal，改为分别输出 x64 与 arm64 dmg/zip（与历史版本一致），从而绕过合并步骤并保证出包成功。
- 修改文件列表：
  - `frontend/electron-builder.yml`
- 完成状态：✅ 已完成（等待出包机重新执行 `npm run electron:build:mac` 验证产物）

---

### 2026-04-29
- 任务类型：测试/维护（打包态）
- 任务简述：为打包态测试准备环境：清理 `dist/electron/` 下旧版本（0.3.0）dmg 产物，避免误测；并整理 mac dmg 的安装/首次启动/health 验证步骤，等待干净机测试反馈后再进入下一步问题定位与修复。
- 修改文件列表：
  - `dist/electron/钻石记忆系统-0.3.0*.dmg*`（删除旧产物）
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：🚧 进行中（等待 mac dmg 冒烟测试结果）

---

### 2026-04-29
- 任务类型：修复/优化/测试（验收缺陷 / OpenClaw）
- 任务简述：修复“刚录入的短偏好句（如：我不喜欢下雨天）OpenClaw 召回不到”的根因：检索后去重过滤器对 L1/L2 统一使用最小 15 字阈值，导致短偏好句被误判为低质量内容直接过滤。调整为：L1/L2 使用更宽松的长度阈值，并对包含偏好关键词（喜欢/不喜欢/偏好/讨厌/习惯…）的短句放行，确保 OpenClaw 的偏好兜底（L1 recent）有效。补齐回归测试覆盖该场景。
- 修改文件列表：
  - `backend/app/services/retrieval_service.py`
  - `backend/tests/test_openclaw_preference_recall_fallback.py`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（backend: `pytest -q backend/tests/test_openclaw_preference_recall_fallback.py::test_preference_query_keeps_short_l1_preference_sentence` 通过）

---

### 2026-04-29
- 任务类型：修复/优化/测试（体验）
- 任务简述：修复“快速整理进度条从开始到结束都不动”的体验问题：为 `MemoryService.quick_organize()` 增加进度回调，将 L1→L2 / L2→L4 / L4→L3 / L4→L6 / L6→L5 各阶段按“待处理任务量/已完成任务量”映射到 5%~95% 的进度区间；并在任务队列执行器中串联回调，确保前端任务面板能实时看到进度变化。补齐后端单测确保存在中间进度更新且单调递增。
- 修改文件列表：
  - `backend/app/services/memory_service.py`
  - `backend/app/services/task_queue_service.py`
  - `backend/tests/test_quick_organize_progress.py`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（backend: `pytest -q` 通过）

---

### 2026-04-29
- 任务类型：修复/优化/测试（验收缺陷）
- 任务简述：修复“切换存储路径提示成功但面板仍显示旧数据”的根因：此前仅更新 `storage_path`，但后端 `--data-dir` 仍固定指向旧数据库。改为“工作区隔离”数据目录：当用户配置存储路径时，后端系统数据目录落到 `storagePath/.diamond/backend-data`；并在设置页切换存储路径后触发后端重启并等待就绪，确保新工作区立即生效。补齐 main 进程路径解析单测，并调整 Vitest 配置覆盖 `frontend/src/main`。
- 修改文件列表：
  - `frontend/src/main/backend-paths.ts`
  - `frontend/src/main/backend-manager.ts`
  - `frontend/src/renderer/views/SettingsView.vue`
  - `frontend/vite.config.ts`
  - `frontend/src/main/backend-paths.test.ts`
  - `frontend/src/main/__tests__/backend-manager.test.ts`
  - `frontend/src/main/__tests__/backend-restart-helpers.test.ts`
  - `frontend/tests/backend-paths.test.mjs`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test` 通过）

---

### 2026-04-29
- 任务类型：修复/优化（体验）
- 任务简述：修复“切换存储路径一直显示切换中/重启很慢/切回旧路径数据变 0”。改造重启链路默认**只重启后端**不停止 Ollama（避免模型重载导致比新开更慢），并为重启增加 30s 超时保护避免 UI 永久卡住。补齐“旧数据目录 → 新工作区数据目录”的自动迁移：当检测到 `userData/backend-data` 有旧数据库而新工作区目录为空时，自动迁移 `memory.db(+wal/shm)` 与 `storage_config.json` 到 `storagePath/.diamond/backend-data`，并将旧目录备份，避免用户切回旧路径数据为 0。
- 修改文件列表：
  - `frontend/src/main/backend-manager.ts`
  - `frontend/src/main/index.ts`
  - `frontend/src/renderer/views/SettingsView.vue`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test` 通过）

---

### 2026-04-29
- 任务类型：优化（工作区切换策略）
- 任务简述：按验收反馈将“切换存储路径”调整为更稳的单一流程：选择新路径后弹出“需要重启”确认；确认后直接执行 **全量重启应用**（关闭旧后端/旧模型/旧端口/旧数据源），再按新路径启动，避免出现“仪表盘统计残留/部分面板仍是旧数据/模型断开后重连不一致”等问题；仅在极端情况下（无 relaunch 能力）回退为仅重启后端。
- 修改文件列表：
  - `frontend/src/renderer/views/SettingsView.vue`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test` 通过）

---

### 2026-04-29
- 任务类型：修复/优化（开发辅助）
- 任务简述：修复“DM开发辅助 一键清除无法关闭开发模式打开的终端窗口”的问题：为开发模式启动的 Terminal 窗口写入唯一标记 `__DM_DEV_MODE_DIAMONDMEMORY__`，并重写 kill_services.sh 的窗口枚举与关闭逻辑（改为换行分隔，避免逗号分隔解析失败；优先按标记精准匹配关闭）。同时修复相关单测路径，确保 `pytest -q DM开发辅助/tests` 通过。
- 修改文件列表：
  - `DM开发辅助/open_dev_mode.sh`
  - `DM开发辅助/kill_services.sh`
  - `DM开发辅助/tests/test_open_dev_mode_order.py`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（DM开发辅助: `pytest -q` 通过）

---

### 2026-04-29
- 任务类型：修复/优化（兜底机制）
- 任务简述：针对“切换存储路径后后端重启超时、模型断开、服务反复断开导致无法稳定进入新工作区”的场景，新增兜底能力：当设置页切换工作区触发的后端重启超过 60s 超时，自动执行 **全量重启应用（app relaunch）**，确保后端/模型/端口/数据目录完全按新路径重新初始化，避免残留旧实例导致串库与断连。该兜底会主动关闭当前窗口并重新打开。
- 修改文件列表：
  - `frontend/src/main/index.ts`
  - `frontend/src/preload/index.ts`
  - `frontend/src/renderer/types/electron.d.ts`
  - `frontend/src/renderer/views/SettingsView.vue`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test` 通过）

---

### 2026-04-29
- 任务类型：修复/优化（迁移策略纠偏）
- 任务简述：修复“切到新空工作区却自动出现旧数据”的迁移误判：旧版本系统数据统一落在 `userData/backend-data`，迁移到工作区数据目录时必须避免误把旧数据库带入新工作区。调整为：仅当旧数据目录 `storage_config.json` 声明的 `storage_path` 与当前配置的存储路径一致时才执行迁移；否则不迁移，保持新工作区为空。补齐迁移判定单测。
- 修改文件列表：
  - `frontend/src/main/backend-migration.ts`
  - `frontend/src/main/backend-migration.test.ts`
  - `frontend/src/main/backend-manager.ts`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test` 通过）

---

### 2026-04-29
- 任务类型：修复/优化（可用性）
- 任务简述：修复“后端断开弹窗遮挡，无法切换到设置页”的可用性问题：当后端断开时全局遮罩不再拦截 Settings 页面；并在断开弹窗中增加“去设置”按钮，确保用户可在断开状态下调整存储路径/查看信息后再重启。
- 修改文件列表：
  - `frontend/src/renderer/App.vue`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test` 通过）

---

### 2026-04-29
- 任务类型：修复/优化（启动体验）
- 任务简述：修复“刚开启一键测试/任务轮询”在后端启动的几秒内触发 `fetch failed` 导致全局 `unhandledrejection` 弹红色 toast 的问题：任务轮询内部捕获并对启动期网络错误静默；全局 `unhandledrejection` 过滤补齐 `fetch failed/ECONNREFUSED` 等变体，避免启动阶段误报。
- 修改文件列表：
  - `frontend/src/renderer/stores/tasks.ts`
  - `frontend/src/renderer/utils/error-handler.ts`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（请你本机启动/打包后快速验证无误报）

---

### 2026-04-29
- 任务类型：修复/优化（验收缺陷）
- 任务简述：为 MVP 验收补齐偏好召回与中文检索兜底：未显式 categories 时根据关键词自动判定偏好查询并启用 L1 兜底；FTS 返回空时追加 LIKE 兜底；切换存储路径时同步更新后端 storage_path（避免导出落到旧路径导致知识库为空）；任务提示入口增加“隐藏任务提示”按钮减少遮挡；OpenClaw 集成指引增加“统计必须走接口、禁止编造”与偏好检索示例
- 修改文件列表：
  - `backend/app/services/retrieval_service.py`
  - `backend/app/storage/sqlite_store.py`
  - `backend/app/services/openclaw_service.py`
  - `frontend/src/renderer/views/SettingsView.vue`
  - `frontend/src/renderer/components/TaskPanel.vue`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（代码与回归测试已补齐；建议你本机用 OpenClaw 再做一次端到端验证）

---

### 2026-04-29
- 任务类型：修复/优化/测试
- 任务简述：ID=20（P0）切换存储路径后知识库/图谱立即更新，并彻底消除 “访问被拒绝：路径不在允许范围内”。统一切换时序：Electron 先落盘 storagePath 并初始化目录；App 层统一触发后端 `storage_path` 更新、知识库导出重建与 UI 刷新；切路径时清空越界的旧文件选中态以避免 IPC 越权；图谱支持 `force=1` 强制重建并在切路径后立即刷新；知识库空时提供“强制重建导出/强制同步”兜底按钮。
- 修改文件列表：
  - `frontend/src/renderer/App.vue`
  - `frontend/src/renderer/views/SettingsView.vue`
  - `frontend/src/renderer/components/FileTree.vue`
  - `frontend/src/renderer/views/KnowledgeGraphView.vue`
  - `frontend/src/renderer/utils/graph-events.ts`
  - `frontend/src/renderer/utils/path-utils.ts`
  - `frontend/src/renderer/utils/path-utils.test.ts`
  - `backend/app/api/memory_routes.py`
  - `backend/tests/test_memory_graph_force_rebuild.py`
- 完成状态：✅ 已完成（frontend: `npm test` / `npm run build` 通过；backend: `pytest -q` 通过）

---

### 2026-04-29
- 任务类型：修复/优化/测试
- 任务简述：ID=15（P0）开发模式“单后端权威”落地：修复 DM开发辅助/open_dev_mode.sh 同时拉起 Electron dev 与源码后端导致“双后端/双端口/双数据源”的根因。默认仅保留 Electron 主进程 BackendManager 管理的后端（写入 port.json 为权威端口来源）；并新增 `DM_BACKEND_MODE=python` 可选模式（脚本启动源码后端 15920，Electron 侧禁止自启动后端、只连接既有后端）。同时修复渲染层在开发模式下误走 Vite proxy 固定端口的问题：只要存在 `electronAPI` 即强制走 IPC（普通请求/上传/流式），确保 UI 与 OpenClaw/命令行始终指向同一 endpoint 与同一 DB。
- 修改文件列表：
  - `DM开发辅助/open_dev_mode.sh`
  - `DM开发辅助/tests/test_open_dev_mode_order.py`
  - `DM开发辅助/tests/test_backend_bootstrap.py`
  - `frontend/src/main/backend-manager.ts`
  - `frontend/src/renderer/api/backend.ts`
  - `frontend/src/renderer/api/backend.test.ts`
  - `frontend/package.json`
  - `frontend/package-lock.json`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（frontend: `npm test`/`npm run build` 通过；DM开发辅助: `pytest -q DM开发辅助/tests` 通过；backend: 关键用例 pytest 通过）

---

### 2026-04-29
- 任务类型：规划/设计
### 2026-04-29
- 任务类型：修复/优化/测试
- 任务简述：完成会话任务卡（ID=19，P1）“拖文件进存储路径自动入库 / 一键同步”：后端将 `POST /api/knowledge/sync` 改为任务队列化（type=knowledge_sync）避免阻塞 UI；新增 `KnowledgeService.sync_user_docs()` 扫描 `用户文档/` 对 PDF/Word/Excel 做增量摄取（复用 file_sync）；前端设置页“同步知识库”按钮改为任务化调用并在完成后强制刷新知识库树（rebuild-memory-exports + FileTree refresh）；补齐后端单测并验证 `pytest -q` 与 `npm -C frontend run build` 通过。
- 修改文件列表：
  - `backend/app/api/knowledge_routes.py`
  - `backend/app/services/knowledge_service.py`
  - `backend/app/services/task_queue_service.py`
  - `backend/app/services/retrieval_service.py`
  - `backend/app/storage/sqlite_store.py`
  - `backend/tests/test_knowledge_sync_enqueue_api.py`
  - `backend/tests/test_knowledge_sync_doc_ingest.py`
  - `frontend/src/renderer/api/backend.ts`
  - `frontend/src/renderer/views/SettingsView.vue`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（backend: `pytest -q` 通过；frontend: `npm run build` 通过）

---

- 任务简述：会话任务卡（ID=19，P1）“拖文件进存储路径自动入库 / 一键同步”方案确认：最小可行先做 Settings/Knowledge 页按钮“同步知识库”，调用 `POST /api/knowledge/sync`；同步走任务队列避免阻塞 UI；同步完成后强制刷新 FileTree，并按需 rebuild-memory-exports。同步范围聚焦 PDF/Word/Excel（用户文档）。
- 修改文件列表：
  - `docs/superpowers/specs/2026-04-29-knowledge-sync-doc-ingest-design.md`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（设计文档已落盘，待进入实现）
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（设计文档已落盘，待进入实现）

---

### 2026-04-29
- 任务类型：规划/设计
### 2026-04-29
- 任务类型：修复/优化/测试
- 任务简述：ID=16（P0）OpenClaw 偏好召回修复落地：修复 `retrieval_service.query()` 未应用 `categories` 过滤导致 preference 被稀释的问题；当 `categories` 包含 `preference` 时启用分层兜底（L4/L6 → L2 → 最近 N 条 L1，并强制走检索后质量过滤/去重/时间排序）；当 embedding 不可用/返回空时进入降级模式：跳过语义检索与精排，强制走关键词检索，并采用“多次单词查询 + 去重合并（依赖 SQLiteStore 的 LIKE 兜底）”提升中文命中率。顺带修复 SQLite FTS 查询实现：避免多次 MATCH 的限制，且中文短词场景下 FTS 为空时自动退化 LIKE。
- 修改文件列表：
  - `backend/app/services/retrieval_service.py`
  - `backend/app/storage/sqlite_store.py`
  - `backend/app/config/settings.py`
  - `backend/tests/test_openclaw_preference_recall_fallback.py`
- 完成状态：✅ 已完成（backend: `pytest -q` 关键回归用例通过）

---

- 任务简述：ID=16（P0）OpenClaw 偏好召回修复设计：修复 retrieval_service.query 未使用 categories 过滤的根因；补齐偏好召回“分层兜底”（L4/L6 → L2 → 最近 N 条 L1 + 质量过滤/去重/时间排序）；补齐 embedding 不可用时的中文关键词兜底（强制走 FTS/LIKE + 常见偏好词扩展），并建议 OpenClaw 固定中文查询词增强与“手动新增 preference 默认写 L2”的体验优化。
- 修改文件列表：
  - `docs/superpowers/specs/2026-04-29-openclaw-preference-recall-fallback-design.md`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（设计文档已落盘，待进入实现）
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成（设计文档已落盘，待进入实现）

---

### 2026-04-29
- 任务类型：修复/优化/测试
- 任务简述：ID=18（P1）采集中心“上传文件/网页采集”接口对齐为 `/api/ingest/*`（后端同时保留 `/api/storage/*` 兼容前缀）；并补齐“免打扰”开关（上传文件：不触发 AI 结构化提炼；手动文本：保留原文排版，不触发自动整理）。前端增强错误提示：当后端返回 `detail`/`error` 时直接 toast 显示可读信息（不再只展示 statusText）。
- 修改文件列表：
  - `frontend/src/renderer/api/backend.ts`
  - `frontend/src/main/http-ipc-proxy.ts`
  - `frontend/src/renderer/types/electron.d.ts`
  - `frontend/src/renderer/views/IngestView.vue`
  - `frontend/src/renderer/views/ChatView.vue`
  - `backend/main.py`
  - `backend/app/api/memory_routes.py`
- 完成状态：✅ 已完成（frontend: `npm run build` 通过；backend: `pytest -q` 通过）

---

### 2026-04-29
- 任务类型：修复/优化/测试
- 任务简述：ID=17（P1）任务面板可关闭/最小化（不遮挡）；有活跃任务时默认显示（自动弹出）；无任务或任务结束自动隐藏。状态持久化：localStorage 保存 `dm-task-panel-hidden`（关闭）与 `dm-task-panel-collapsed`（最小化）；当用户选择关闭时，面板以“任务计数启动器”形式保留入口，方便运行中随时手动展开查看。并修复前端 build 阶段 TypeScript 严格模式报错、排除测试文件参与 build。
- 修改文件列表：
  - `frontend/src/renderer/components/TaskPanel.vue`
  - `frontend/src/renderer/stores/tasks.ts`
  - `frontend/src/renderer/components/FileTree.vue`
  - `frontend/src/renderer/components/UpdateNotification.vue`
  - `frontend/tsconfig.json`
- 完成状态：✅ 已完成（frontend: `npm run build` 通过）

---

### 2026-04-29
- 任务类型：规划/验收
- 任务简述：根据验收反馈补充“验收缺陷修复”任务（ID=15~20）：多后端导致数据不一致、OpenClaw 召回偏好失败（含 L1 兜底/中文关键词兜底）、任务面板可关闭、采集上传失败与免打扰开关、手动拖入文档自动入库、切换存储路径后知识库/图谱不同步与访问被拒绝
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成

---

### 2026-04-29
- 任务类型：同步/规范
- 任务简述：同步更新任务总览表状态：根据并行会话反馈，将 ID=10（MCP 接入体验产品化）、ID=12（Local-first 降级可用性）更新为 ✅ 已完成，确保总览表与实际进度一致
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：✅ 已完成

---

### 2026-04-29
- 任务类型：修复/优化/测试
- 任务简述：深度安全加固（二期）：前端主进程增加 `will-navigate`/`setWindowOpenHandler` 拦截外链与非预期导航（外链转交系统浏览器）；后端补齐知识库文件读写的路径防穿越（统一 safe resolve）、限制 knowledge API 的 `root_path` 不得越界；存储路径与系统数据目录更新接口增加服务端路径校验（禁止根目录）；URL 摄取接口增加基础 SSRF 防护（禁止 localhost/内网 IP）。新增安全回归单测覆盖路径穿越/越界/root 目录/本机 URL 等场景。
- 修改文件列表：
  - `frontend/src/main/index.ts`
  - `backend/app/api/ingest.py`
  - `backend/app/api/knowledge_routes.py`
  - `backend/app/services/knowledge_service.py`
  - `backend/app/api/storage_routes.py`
  - `backend/app/api/config_routes.py`
  - `backend/tests/test_security_hardening_paths.py`
- 完成状态：已完成（frontend: `npm run build` 通过；backend: `pytest -q` 通过）

---

### 2026-04-29
- 任务类型：修复/优化
- 任务简述：安全与稳定性加固：为高危操作（卸载/删模型）增加确认弹窗；存储路径写入增加校验（禁止根目录等误配置）；重做“项目备份”实现（异步等待、超时控制、跨平台支持、项目内 `项目备份/` 优先并保留最近 10 份）；后端补齐模型拉取接口 `model_name` 输入校验与锁使用规范，并修复后台启动 Ollama 等进程时 `stdout/stderr=PIPE` 可能导致的阻塞风险（改为 DEVNULL）。
- 修改文件列表：
  - `frontend/src/main/index.ts`
  - `backend/app/api/config_routes.py`
  - `backend/app/services/inference/inference_service.py`
  - `backend/app/services/ollama_download_service.py`
- 完成状态：已完成（frontend: `npm run build` 通过；backend: `pytest -q` 通过）

---

### 2026-04-29
- 任务类型：修复
- 任务简述：会话任务卡#2（P0）前端传参链路修正：后端启动参数 `--data-dir` 强制固定为 `userData/backend-data`（系统运行时写入根），`--storage-path` 才使用用户配置路径（未配置则回退到 data-dir）；新增纯函数路径解析模块与 Node 单测，避免未来回归。
- 修改文件列表：
  - `frontend/src/main/backend-manager.ts`
  - `frontend/src/main/backend-paths.ts`
  - `frontend/tests/backend-paths.test.mjs`
- 完成状态：已完成（`npm -C frontend run electron:build-main` 通过；`node --test frontend/tests/backend-paths.test.mjs` 通过）

---

### 2026-04-29
- 任务类型：优化
- 任务简述：补齐会话任务卡（ID=9，P0）“管理/高级设置入口”：在设置页新增“允许查询历史版本”开关，接入 `/api/config/get|set` 并将开关值同步到后端 runtime settings（重启可从 DB 配置恢复），用于放行 `include_history` 与版本链/状态切换能力。
- 修改文件列表：
  - `backend/app/api/config_routes.py`
  - `frontend/src/renderer/views/SettingsView.vue`
- 完成状态：已完成（backend: `pytest -q` 通过；frontend: `npm run build` 通过）

---

### 2026-04-29
- 任务类型：优化/修复
- 任务简述：会话任务卡（ID=9，P0）时序与冲突治理：统一“有效版本”过滤（默认仅 active 且 invalid_at 为空），补齐检索侧 include_history 管理开关；增强 supersede 审计信息（score/规则）；补充管理接口（版本链查询/切换状态）；并新增单测覆盖历史过滤。顺带修复 `mcp_self_check_service` 缺少模块级 `run_self_check()` 导致单测失败问题。
- 修改文件列表：
  - `backend/app/storage/sqlite_store.py`
  - `backend/app/services/retrieval_service.py`
  - `backend/app/services/memory_service.py`
  - `backend/app/api/memory_routes.py`
  - `backend/app/config/settings.py`
  - `backend/app/services/mcp_self_check_service.py`
  - `backend/tests/test_retrieval_history_filter.py`
- 完成状态：已完成（本环境 `pytest -q`：55 passed, 1 skipped）

---

### 2026-04-29
- 任务类型：修复/重构
- 任务简述：按“最彻底方案B”落地 IPC→主进程→HTTP 代理：生产环境渲染进程不再直连本机后端，覆盖普通接口/聊天流式/文件上传/URL采集，从根因绕开 CORS（Origin null/file://）
- 修改文件列表：
  - `frontend/src/main/http-ipc-proxy.ts`
  - `frontend/src/main/index.ts`
  - `frontend/src/preload/index.ts`
  - `frontend/src/renderer/api/backend.ts`
  - `frontend/src/renderer/types/electron.d.ts`
  - `frontend/src/renderer/views/ChatView.vue`
  - `frontend/src/renderer/views/IngestView.vue`
  - `frontend/src/renderer/views/MemoryView.vue`
  - `frontend/src/renderer/stores/knowledge.ts`
  - `docs/superpowers/specs/2026-04-29-electron-ipc-http-proxy-design.md`
  - `docs/superpowers/plans/2026-04-29-electron-ipc-http-proxy.md`
- 完成状态：已完成（本环境已通过 tsc/vue-tsc/vite build；electron-builder 受网络下载 Electron 依赖影响未完成，需在本机正常网络下打包冒烟验证）

---

### 2026-04-29
- 任务类型：规范
- 任务简述：版本优化总览表状态统一增加图标（✅/🚧/⏳/⛔/🗑️），便于快速扫视；并将该规则纳入全局规则文件
- 修改文件列表：
  - `.trae/rules/project_rules.md`
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：重构/规范
- 任务简述：按全局新规则调整版本优化文档结构：任务总览表置顶；下方任务记录按时间倒序；并同步更新总览表状态（本次补齐 ID=4/11 状态为已完成）
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
  - `.trae/rules/project_rules.md`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：新增/优化
- 任务简述：会话任务卡 #4（ID=4，P1）文档入库双管道：新增保真索引(A)（文档块级 chunk + page/offset/hash/path/mtime）与结构化索引(B)（LLM 提炼 + citations 回指 A）；并支持免打扰参数 `disturb_free` 只走 A；检索结果透出 `citations` 以引用到具体段落。
- 任务类型：新增/规范
- 任务简述：新增“结束本轮优化自动备份完整项目”全局规则：当用户说“本次版本优化结束”时，自动在项目根目录 `项目备份/` 下生成一次完整项目备份；并提供脚本 `scripts/backup_project.sh`（默认排除可再生大目录，保留最近 10 个）
- 修改文件列表：
  - `.trae/rules/project_rules.md`
  - `scripts/backup_project.sh`
  - `项目备份/README.md`
- 完成状态：✅ 已完成

---

### 2026-04-29
- 修改文件列表：
  - `backend/app/services/ingest/doc_blocks.py`
  - `backend/app/services/ingest/ingest_service.py`
  - `backend/app/services/ingest/pdf_parser.py`
  - `backend/app/services/ingest/ingest_service.py`
  - `backend/app/services/ingest/pdf_parser.py`
  - `backend/app/services/ingest/doc_parser.py`
  - `backend/app/services/ingest/excel_parser.py`
  - `backend/app/api/ingest.py`
  - `backend/app/services/retrieval_service.py`
  - `backend/app/services/memory_service.py`
  - `backend/tests/test_doc_ingest_dual_pipeline.py`
  - `docs/superpowers/plans/2026-04-29-doc-ingest-dual-pipeline.md`
- 完成状态：已完成（新增单测通过：`tests/test_doc_ingest_dual_pipeline.py`）

---

### 2026-04-29
- 任务类型：修复
- 任务简述：修正 Backlog 表格状态未同步问题：将已完成的任务（ID=2/5/7/8）更新为“已完成”，确保表格与上方任务记录一致
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：优化
- 任务简述：会话任务卡（ID=8，P2）端口与配置统一：增强 `BackendManager` 的端口落盘与兼容读取逻辑——`port_config.json` 为权威策略；`userData/port.json` 与 `~/.diamond-memory/port.json` 双路径原子写并补齐 `version/updatedAt`；当 userData 配置缺失/损坏时按 “userData → home → 默认端口” 降级恢复，避免外部工具读到旧端口。
- 修改文件列表：
  - `frontend/src/main/backend-manager.ts`
  - `backend/app/services/openclaw_service.py`
  - `backend/app/services/qclaw_service.py`
  - `docs/superpowers/plans/2026-04-29-port-config-unification.md`
- 完成状态：已完成（前端主进程 TS 编译通过；后端 pytest 当前环境存在既有失败用例，未纳入本任务修复范围）

---

### 2026-04-29
- 任务类型：规划
- 任务简述：会话任务卡 #8（ID=8，P2）端口与配置统一：确定以 `userData/port.json` 为权威端口来源，并镜像写入 `~/.diamond-memory/port.json`；补齐启动读优先级（userData → home → default）、双文件不一致时以 userData 覆盖 home、原子写等设计约束，形成可执行设计稿。
- 任务类型：优化
- 任务简述：会话任务卡 #8（ID=8，P2）端口与配置统一：启动时端口发现优先读取 `userData/port.json`，fallback 再读 `~/.diamond-memory/port.json`；并在启动早期同步 home 镜像（以 userData 覆盖，避免旧文件误导）。同时更新 OpenClaw/QClaw/Hermes 集成说明，明确 home 端口文件为兼容镜像、权威来源为 userData。
- 修改文件列表：
  - `frontend/src/main/backend-manager.ts`
  - `backend/app/services/openclaw_service.py`
  - `backend/app/services/qclaw_service.py`
  - `backend/app/services/hermes_service.py`
- 完成状态：已完成（electron main ts 编译通过；frontend build 通过；Python compileall 通过）

---

### 2026-04-29
- 修改文件列表：
  - `docs/superpowers/specs/2026-04-29-port-config-unification-design.md`
- 完成状态：已完成（设计稿已提交，待进入实现计划）

- 完成状态：已完成（设计稿已提交，待进入实现计划）

---

### 2026-04-29
- 任务类型：新增/优化
- 任务简述：会话任务卡（ID=11，P1）L6 技能层产品化（最小可行）：为 L6 技能补齐元数据 schema（skill_id/version/source/preconditions/tools/steps/expected_output），新增调用统计与评分反馈指标，并实现“阈值触发自动升级任务 + 生成 v+1 + 保留旧版本链”与手动升级接口；同时补齐技能相关 API 与升级任务队列表。
- 修改文件列表：
  - `backend/app/services/skill_service.py`
  - `backend/app/api/skill_routes.py`
  - `backend/app/storage/sqlite_store.py`
  - `backend/app/services/memory_service.py`
  - `backend/app/config/settings.py`
  - `backend/main.py`
  - `backend/tests/test_skill_productization.py`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：优化
- 任务简述：会话任务卡（ID=6，P2）：KnowledgeService 支持 layer_filter 真正生效（层级→根目录映射剪枝），并增加大目录性能保护：基于 file_sync 最大 mtime + 根目录 mtime 签名缓存；递归扫描加入 per_dir_limit / max_depth 限流；新增分页子目录接口用于“加载更多”；前端文件树（IPC 本地浏览）增加分页加载更多，避免一次性读入上万条导致卡顿。
- 修改文件列表：
  - `backend/app/services/knowledge_service.py`
  - `backend/app/api/knowledge_routes.py`
  - `backend/tests/test_knowledge_service_scan_file_tree.py`
  - `frontend/src/main/index.ts`
  - `frontend/src/preload/index.ts`
  - `frontend/src/renderer/types/electron.d.ts`
  - `frontend/src/renderer/components/FileTree.vue`
- 完成状态：已完成（单测通过；大目录模拟 1w 文件二次打开显著更快，且支持“加载更多”分页）

---

### 2026-04-29
- 任务类型：新增/优化
- 任务简述：固化统一测试规范，并为 DM开发辅助新增“冒烟测试”按钮与脚本（开发态快速验证核心链路）
- 修改文件列表：
  - `DM开发辅助/smoke_test.sh`
  - `DM开发辅助/DM开发辅助/ContentView.swift`
  - `PROJECT_TESTING_GUIDE.md`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：修复
- 任务简述：会话任务卡#2（P0）：禁止运行时写安装目录。后端在 import 阶段优先识别 `--data-dir`/env 兜底，所有运行时写入（storage_config/db/索引/qdrant/temp/backups）统一落到 data_dir（userData）；保留旧位置 storage_config 读取兼容，并在新位置缺失时自动迁移写入；彻底移除对 `project_root/storage_config.json` 的写入。
- 修改文件列表：
  - `backend/app/config/settings.py`
  - `backend/tests/test_userdata_data_dir_resolution.py`
- 完成状态：已完成（新增回归测试通过；关键后端测试通过）

---

### 2026-04-29
- 任务类型：优化
- 任务简述：会话任务卡#5：L2/L4/L6 去重升级为 merge vs supersede（版本链/时序）。去重命中后高相似走增量合并并写审计；纠错/中等相似走 supersede（旧条 invalid，新条 active，parent_id/superseded_by 形成版本链）；检索侧默认过滤 inactive；补充版本链/审计日志存储方法与索引；新增单测覆盖 merge/supersede。
- 修改文件列表：
  - `backend/app/services/memory_service.py`
  - `backend/app/services/retrieval_service.py`
  - `backend/app/storage/sqlite_store.py`
  - `backend/tests/test_memory_dedup_merge_supersede.py`
- 完成状态：已完成（新增单测通过；历史版本链可通过 SQLiteStore.get_version_chain 查询）

---

### 2026-04-29
- 任务类型：修复
- 任务简述：后端向量引擎选择修正：MemoryService/RetrievalService/KnowledgeService 统一使用 get_active_vector_store；settings 选 qdrant 时优先走 QdrantVectorStore，不可用时自动降级（内部 fallback）；检索侧补齐 status=active 校验；新增单测覆盖 /health 引擎展示。
- 修改文件列表：
  - `backend/app/storage/__init__.py`
  - `backend/app/services/memory_service.py`
  - `backend/app/services/retrieval_service.py`
  - `backend/app/services/knowledge_service.py`
  - `backend/tests/test_vector_engine_selection.py`
- 完成状态：已完成（/health → services.vector_store.engine 可显示 qdrant；qdrant-client 不可用时显示 faiss）

---

### 2026-04-29
- 任务类型：测试
- 任务简述：打包链路验证（mac dmg + win nsis）自动冒烟：启动 App → 等后端 health → 调用创建/检索 API → 退出；并校验 resourcesPath 内 backend/ollama 资源齐全
- 修改文件列表：
  - `frontend/src/main/index.ts`
  - `scripts/smoke/smoke_app.mjs`
  - `scripts/smoke/smoke_mac_dmg.sh`
  - `scripts/smoke/smoke_win_nsis.ps1`
  - `DM开发辅助/create_dmg.sh`
  - `DM开发辅助/create_exe.bat`
- 完成状态：已完成（支持 CI/干净机一键运行；本机已通过 TS 编译校验）

---

### 2026-04-29
- 任务类型：优化
- 任务简述：KnowledgeService 扫描按层过滤（layer_filter）真实生效；引入 file_sync + 浅层签名缓存，避免大目录下频繁全量递归扫描导致卡顿
- 修改文件列表：
  - `backend/app/services/knowledge_service.py`
  - `backend/app/storage/sqlite_store.py`
  - `backend/tests/test_knowledge_service_scan_file_tree.py`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：规划
- 任务简述：前 8 项任务已并行启动，补发剩余任务（ID=6/8/11/12）执行会话任务卡
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：修复
- 任务简述：修复生产环境 file:// / Origin: null 场景下的 CORS；生产环境仅允许本机环回访问以降低风险
- 修改文件列表：
  - `backend/main.py`
  - `backend/app/config/settings.py`
- 完成状态：已完成（待打包冒烟：创建记忆/查询记忆/知识库树/聊天无 CORS 报错）

---

### 2026-04-29
- 任务类型：规划
- 任务简述：结合《价值定位与竞品对比》补充/调整 V0.9.1 优化 Backlog（强化时序冲突治理、MCP 接入体验、L6 技能演化）
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：规划
- 任务简述：将 V0.9.1 拆分为 8 个可并行执行的独立优化任务卡（每会话一项）
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：已完成

---

### 2026-04-29
- 任务类型：新增
- 任务简述：创建 V0.9.1 版本优化记录文档，并初始化优化项清单
- 修改文件列表：
  - `版本优化记录/版本优化0.9.1.md`
- 完成状态：已完成

---

## 风险与注意事项
1. macOS 若要“普通用户双击即开”，需要签名与公证（notarize）；否则会被 Gatekeeper 拦截。
2. Windows Nuitka/依赖库可能引入运行库要求，需要在纯净环境验证并补齐运行库策略。
3. 数据迁移（storage_path/data_directory）涉及用户数据安全：必须提供备份/回滚点。
